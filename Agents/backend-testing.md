# Backend Testing - facture

Use this skill when backend or API services are introduced.

## Current State
There is no backend in `facture` today, so backend testing is not applicable yet.

## Future Baseline
- Unit tests for pure service logic
- Integration tests for API routes and persistence
- Contract tests for frontend/backend payloads
- Migration tests once a database exists
- Auth and authorization regression tests before any protected data ships
